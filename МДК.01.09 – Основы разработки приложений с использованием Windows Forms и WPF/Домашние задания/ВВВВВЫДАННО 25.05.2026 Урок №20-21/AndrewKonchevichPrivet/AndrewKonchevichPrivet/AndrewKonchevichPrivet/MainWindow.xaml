<Window x:Class="AndrewKonchevichPrivet.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Заметки" Height="500" Width="550"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="✨ПИРОГ✨ — МОИ ЗАМЕТКИ" 
                   FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10"/>


        <StackPanel Grid.Row="1" Margin="0,10">
            <TextBlock Text="Заголовок:" FontSize="14" FontWeight="Bold"/>
            <TextBox x:Name="TitleBox" Height="35" FontSize="14" Margin="0,5" BorderBrush="Gray" BorderThickness="1"/>
        </StackPanel>


        <StackPanel Grid.Row="2" Margin="0,10">
            <TextBlock Text="Текст заметки:" FontSize="14" FontWeight="Bold"/>
            <TextBox x:Name="ContentBox" Height="92" FontSize="14" 
                     TextWrapping="Wrap" AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     BorderBrush="Gray" BorderThickness="1"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,30">
            <Button x:Name="AddBtn" Content="Добавить" Width="120" Height="40" 
                    Background="LightGreen" FontSize="14" FontWeight="Bold"
                    Click="AddBtn_Click" Margin="5"/>
            <Button x:Name="ClearBtn" Content="Очистить" Width="120" Height="40"
                    Background="Orange" FontSize="14" FontWeight="Bold"
                    Click="ClearBtn_Click" Margin="5"/>
        </StackPanel>

        <GroupBox Grid.Row="50" Header="Список заметок" Margin="0">
            <ListBox x:Name="NotesList" Height="70" FontSize="13"
                     SelectionChanged="NotesList_SelectionChanged"/>
        </GroupBox>

    </Grid>
</Window>